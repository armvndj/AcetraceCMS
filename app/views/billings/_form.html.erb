<%= form_with(model: billing, local: true) do |form| %>
  <% if billing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(billing.errors.count, "error") %> prohibited this billing from being saved:</h2>

      <ul>
      <% billing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cost %>
    <%= form.text_field :cost, id: :billing_cost %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :billing_description %>
  </div>

  <div class="field">
    <%= form.label :payment %>
    <%= form.text_field :payment, id: :billing_payment %>
  </div>

  <div class="field">
    <%= form.label :mycase_id %>
    <%= form.text_field :mycase_id, id: :billing_mycase_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
